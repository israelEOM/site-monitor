<script setup>
import { Link } from '@inertiajs/vue3';

defineProps({
    links: {
        type: Array
    }
});

</script>

<template>
    <div v-if="links.length > 3">
        <div class="flex flex-wrap">
            <template v-for="(link, key) in links" :key="key">
                <div
                    v-if="link.url === null"
                    class="mr-1 mb-1 px-4 py-3 text-sm leading-4 text-gray-400 border rounded-lg"
                    v-html="link.label"
                />

                <Link
                    v-else
                    class="mr-1 mb-1 px-4 py-3 text-sm leading-4 border rounded-lg hover:bg-gray-900 focus:border-primary focus:text-primary"
                    :class="{ 'bg-gray-500 text-white': link.active }"
                    :href="link.url"
                    v-html="link.label"
                />
            </template>
        </div>
    </div>
</template>